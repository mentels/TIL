## Choose version of a file when both added

To choose version from the branch onto which rebasing:
`git checkout --ours MY_FILE`

To choose version from the branch being rebase:
`git checkout --theirs MY FILE`


